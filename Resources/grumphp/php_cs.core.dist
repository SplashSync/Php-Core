<?php


$finder = PhpCsFixer\Finder::create()
    ->exclude('tests/Fixtures')
    ->exclude('vendor')
    ->in(dirname(dirname(__DIR__)))
//    ->in(dirname(dirname(__DIR__)) . "/Core")
;

include_once "php_cs.rules.php";

return $config;
